apiVersion: v1
kind: Service
metadata:
  name: api-gw-tenant1
spec:
  type: ClusterIP
  selector:
    app: api-gw-tenant1
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
